# Isaac Sim 5.1.0 bundled Python 3.11 target
# numpy / torch / gymnasium / protobuf version pins removed — bundled versions win

wandb>=0.18.0
tensorboard
matplotlib
tqdm
pyyaml
pillow
scipy
pytest

# isaac_lab/yolo_eval.py — real YOLOv8 eval + vision-calibration in the
# training image (TiledCamera renders + drone_bombard_best.pt inference).
# opencv-python is already pulled in transitively by ultralytics.
ultralytics
