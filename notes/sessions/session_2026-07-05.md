---
date: 2026-07-05
tags: [session, isaac-lab, physics-fix, ablation, forensics]
status: done
type: session
---

# 세션 2026-07-05 — plant 수정 실행 + inertia 대반전 + exp_014 A2/A0′

> 사용자 지시(ultracode): 확정 버그 2건 기계적 수정 → `--zero-actions` 하드 게이트 →
> probe 3종 교차검증 → 판정 게이트(수렴 시 A1 생략, A2 직행). **reward_success·
> entropy_coef 불변 유지.** 전 항목 완수 + 예정에 없던 inertia 대발견.

## 타임라인

1. **수정 커밋 `cd0c617`**: 스폰타임 `UsdPhysics.MassAPI` authoring(킥 제거) +
   로터 ±200 rad/s 리셋 재주입 제거(`init_state` 오버라이드). `7657df4`:
   `Episode_Metric/action_sat_frac` 로깅(item 5).
2. **하드 게이트 PASS**: `--zero-actions` 드리프트 0.179 m (이전 11.9 m FAIL).
3. **Probe A 이상 신호**: 구 정책 재평가 → bad_attitude 0.5%→68% 폭증.
   프롭스핀 A/B로 로터 수정 무관 실증 → inertia 추적.
4. **`_diag_inertia.py` 계측 (세션 최대 발견)**: `set_inertias`는 solver에
   **전파된다**(×1031 확인) — "전파 안 됨" 통설 오류. exp_013은 rate loop
   ~1300× 저토크 plant에서 학습됐던 것. → 커밋 `7d0e9b6`: x500 inertia(0.0217)
   authoring으로 최초의 일관 plant. 게이트 재PASS(0.218 m).
   [[research/isaac_inertia_ctrl_mismatch]] / Rule 19 신설.
5. **Probe B/C** (최종 plant, 200 ep): climb 잔존 22.5%·전부 spawn 직후
   +3 m/s 명령 rail(물리 배제), 감쇠 반사실로 climb 수익 ×0.12-0.16.
   **세 probe 수렴 → A1 생략, A2 직행.**
6. **캘리브레이션 차단**: `yolo_eval.py --calibrate` 하네스 전면 수리(X-마커
   쿼드·카메라 spawn·스윕 하드닝, 커밋 `5343e38`)했으나 TiledCamera annotator가
   이미지 레벨에서 크래시(stock 예제 동일) → 분석 커브로 진행, 캘리브레이션은
   이미지 수정 후 필수.
7. **A2 학습** (400 iters, wandb `v3qk07pg`): **R_alt(300-400)=0.0000**,
   success 99.85%, noise_std 0.80 안정(폭주 없음). climb 모드가 iter 150-199에
   27.8%로 창발했다가 **완전 기각** — attractor 탐색-후-포기의 직접 증거.
8. **A0′ 대조군** (감쇠 없음, wandb `azoc1xp0`): R_alt 0.0365(후반 재창발 기미,
   300-349 버킷 5.8%), success 96.5%, rew_vision ×5.4 → **지배 요인은 plant
   일관성, 감쇠는 잔여 꼬리 제거 + parity** ([[experiments/exp_014_A2_visionrange]] §5).
9. **A2 deterministic 200-ep eval: 100.0% (202/202)**, d_xy_min 0.665 m,
   drop_impact_error 4.59 m (exp_013: 36% / 1.43 m / 24 m).

## 발견/정정 요약

- **정정**: "set_inertias 전파 안 됨"(07-04 포렌식·코드 주석) → 오류. 전파됨.
- **정정**: exp_013의 36%는 "킥 오염"이 아니라 **자세-마비 plant**의 수치.
  구 체크포인트는 새 plant에서 무효(Rule 19c).
- **재해석**: noise_std 폭주(Rule 18b)도 상당 부분 저토크 plant 아티팩트 —
  일관 plant에선 entropy_coef 0.005 그대로도 σ 0.80 안정.
- 컨테이너 이슈 3건 기록: annotator 버그(이미지), wandb 키(`--env-file`),
  simulation_app.close() 행 프로세스 → [[isaac_container_access]] 메모리 갱신.

## 산출물

- 커밋: `cd0c617`, `7657df4`, `7d0e9b6`, `5343e38` (+ 세션 종료 docs 커밋)
- 신규 도구: `_diag_inertia.py`, `_probe_traj.py`, `_probe_analyze.py`,
  `_fit_falloff.py`, `_extract_traincurve.py`
- 노트: [[research/isaac_inertia_ctrl_mismatch]], [[experiments/exp_014_A2_visionrange]],
  Rule 19, 각 허브 갱신

## 다음 세션 (사용자 결정 대기)

- reward_success 100→300, entropy_coef 조정 — **별도 페이즈** (이번 세션 금지 항목).
  단, 일관 plant에서 noise_std가 안정이므로 entropy_coef 0 근거는 재평가 필요.
- success_radius 0.8→0.5 커리큘럼 (성공 99.85%는 0.8 기준 — 조일 여지 큼).
- 이미지 수정 후 `yolo_eval.py --calibrate` 재실행 → 감쇠 커브 실측치로 교체.
- `drop_impact_error_m` 중심의 임무 지표 평가.

---

## 저녁 세션 — exp_016 (ultracode: 4.59 m 디커플링 최우선 규명)

- 사용자 지시: success 100% vs drop_impact_error 4.59 m 디커플링 규명, 릴리스
  조건이 (a) d_xy 기반이면 버그로 보고 0.2 m CCIP 트리거로 수정 + 200-ep 재평가.
  reward_success/entropy/비전 캘리브레이션 불변(범위 밖).
- **결과**: (a) 확정 — 트리거 부재, 지표가 성공-종단 속도 캐리 측정(3.0 m/s×1.53 s≈4.6 m).
  CCIP referee 수정(지표 전용) 후 재평가: 발화 시 0.137 m, **release_rate 6%** —
  aim_err_min med 0.755 m ≈ d_xy_min(근접 정책 ≠ 릴리스 능력). 구 지표 4.649 m 재현.
  적대 검증 5/5 확정. Rule 21 신설.
- 위 "다음 세션" 항목 중 **`drop_impact_error_m` 중심 임무 지표 평가**는 본 세션에서
  지표 의미론 수정까지 완료 — 이후 실 능력 개선은 exp_015 Phase 2 학습의 몫.
- → [[experiments/exp_016_ccip_release_reeval]] / [[research/ccip_release_decoupling]] / [[daily/daily_2026-07-05]]
