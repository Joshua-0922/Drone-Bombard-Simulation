---
date: 2026-07-06
tags: [daily, isaac-lab, reward-design, ccip]
status: done
type: daily
---

# Daily — 2026-07-06

> **오후 추가 — exp_018 Stage B (릴리스-종단): release_rate 5.5% → 100%.**
> 근접 종단을 릴리스-발화 종단으로 교체(`release_terminal` cfg, 게이트 불변) +
> aim_err 보상 nominal-전용 고정. **종단 교체 단독(B0)으로 학습 내 23→99.6%
> 단조 상승**(Stage A 하락 반전 — Rule 22a 인과 확정), det 100.00%·drop err
> 0.125 m·호버-드롭 수렴. 스윕: w_aim 0/1.5 모두 100%(aim 항 잉여 — 자동 발화
> referee가 노이즈를 +100 샘플러로 전환), knee 0.75만 98.5%. 4-lens 적대 검증이
> done-flag **alias 버그 사전 발견**(eval success 0% 위험 → `.clone()`, Rule
> 23d). ckpt 4종 + 호스트 백업(`checkpoints/exp018/`). **Stage C(DR+residual,
> 별도 지시) warm-start = `exp018_B0_final.pt`.** Rule 23 신설.
> → [[experiments/exp_018_release_terminal]] / [[research/release_terminal_stageB]]

## 오늘 한 일 (오전 — Stage A)

- **exp_017 Stage A 전체 사이클** (지시: 보상 변경만으로 release_rate@0.2m을 6%
  기준선 위로): 구현 → 5-lens 적대 검증 → P1 6-dim 기준선 학습(750gpldr) →
  v1(w_aim=1, 6z0gpnhy) → v2(w_aim=2/knee 1.0, fv5qqmtz) → 각 200-ep
  deterministic eval → 문서화·커밋.
- 밀집 CCIP 조준 보상 `w_aim·(1-tanh(aim_err/s))` 구현 (referee와 동일 aim_err,
  cfg 기본 off, `--w_aim` CLI, `aim_error_reward` 순수 함수 + 테스트 40/40).
- 체크포인트 3종 분리 보존 + 호스트 백업(`/opt/drone-bombard/checkpoints/exp017/`).

## 주요 결정 & 발견

- **판정 (b) — 보상 변경 단독으로는 정체.** det release_rate 2.5%(기준선) →
  5.5%(v1; aim_err_min med 0.889 m·speed 2.72 m/s로 방향 실재, 단 n=200 p≈0.13)
  → 3.5%(v2 회귀). 6% 참조선을 유의하게 못 넘음.
- **P1 기준선의 역설**: 학습 내 release_rate 12→3.7% 단조 하락 — 근접 최적화가
  릴리스 능력을 **능동적으로 파괴**한다 (경로 cross-track이 CCIP 최근접을 결정).
- **구조 원인 3종 (Rule 22)**: ①γ=0.995 할인 +100이 모든 감속을 벌함 ②CCIP가
  탐험 σ(1.1→1.55)를 ×1.53 s 증폭 → dense 그래디언트 평탄화 ③성공 조기 종단이
  조준 구간 제거. → **릴리스는 Phase 2 릴리스-종단 구조로 학습해야** (별도 지시 대기).
- 5-lens 검증 중 pathology가 w=2 duty-cycle 펌프 PV(~137–180 vs 완주 ~149) 경고
  → w=1 헤지 개시. 실측 farm 0 (에피소드 길이·timeout·stagnation 전부 정상).
- plumbing 검증 실증 2건: rsl_rl warm-start는 max_iterations **추가** 실행(399→799);
  `--final_out`은 무조건 덮어씀 → run별 경로 분리 필수.

## 코드 변경 사항 (커밋 예정)

- `isaac_lab/drone_bombard/math_utils.py`: `aim_error_reward` 신설.
- `isaac_lab/drone_bombard/drone_bombard_env.py`: `RewardCfg.w_aim/aim_reward_scale`,
  `_aim_err_last` 버퍼(양 referee 경로 스태시), `_get_rewards` 배선, `rew_aim`
  episode sum, reset.
- `isaac_lab/train.py`: `--w_aim/--aim_reward_scale` + 오케스트레이터 passthrough.
- `isaac_lab/tests/test_math.py`: aim 테스트 2종 (40/40 PASS).
- notes: `research/ccip_aim_reward_stageA`, `experiments/exp_017_stageA_aim_reward`,
  rl_rules Rule 22, training_history·00_index·RL_Project_Log 갱신.

## 문제 & 해결

- 컨테이너 로그 dir 권한(exec 기본 uid 1234 vs root-owned `/workspace/logs`) →
  `docker exec -u root`로 학습 실행 (기존 run들도 root였음). `isaaclab.sh`가
  Traceback을 exit 0으로 삼킴 → 로그 grep 감시로 대체.
- 학습 내 지표가 σ-지터에 가려 평탄 → deterministic eval로 판정 (Rule 22c).

## 내일 할 일

- ~~Stage B: 릴리스-종단 학습~~ → **같은 날 완료 (상단 오후 추가 참조).**
- (사용자 별도 지시 대기) Stage C: Phase 2 본선(DR drag/wind + residual) —
  warm-start `exp018_B0_final.pt`. release_tolerance 0.2(referee) vs 0.5(Phase 2
  기본) 정합, 호버-드롭 프로파일의 바람 강건성 검증 포함.
- 실 YOLO 캘리브레이션(annotator 버그)은 여전히 차단 상태.

## 관련 노트

[[experiments/exp_017_stageA_aim_reward]] · [[research/ccip_aim_reward_stageA]] ·
[[research/rl_rules]] Rule 22 · [[daily/daily_2026-07-05]] · [[00_index]]
