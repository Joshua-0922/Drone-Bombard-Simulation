---
date: 2026-08-01
tags: [daily, log, git, branch-cleanup, graphify]
type: daily
status: complete
---

# 연구 일지 — 2026-08-01

---

## 오늘 한 일

- **graphify + context7 설치** (`.graphifyignore`, `CLAUDE.md` graphify 섹션, `.gitignore` 항목) — `Isaac-JS` 브랜치에 커밋.
- **`Isaac-JS`에서 `git push` 시도 중 "용량 초과" 보고 → 전체 브랜치(10개) 계보 조사.** `Isaac-JS`/`isaac_jk`/`Issac_JS`/`feat/isaac-env-migration`/`jekyun`/`jekyun_v2`/`donghyeok`/`junsang`/`main`/`legacy/gazebo-classic` 각각의 조상 관계·고유 커밋을 `merge-base`/`rev-list`로 전수 확인.
- **push 실패 원인 확정:** repo가 아니라 `Isaac-JS`의 마지막 커밋(`f56c9d6`, graphify 추가)이 `git add .`로 넓게 잡히면서 SAC replay buffer(.pkl 66.8MB×2)·eval mp4·zip 등 ~166MB를 실수로 같이 커밋한 것. (서버에 이미 동일 블롭이 있어 실제 push 자체는 통과됨 — 근본 bloat는 YOLO epoch 체크포인트 9종(158MB)·중복 best.pt·`notes/.obsidian/plugins/dataview/main.js` 등 여러 브랜치 히스토리에 누적된 것.)
- **`Isaac-JS` vs `isaac_jk` 실체 규명:** `Isaac-JS`는 2026-07-02(`3d7e2b6`) 이후 `isaac_lab/` 코드를 전혀 받지 못한 순수 Gazebo/PX4/SAC 트랙 브랜치(파일 0개 확인). "v19까지 좋은 결과" 인상은 `daily_2026-07-07.md`가 **기록 전용**으로 다른 워크트리(`feat/isaac-env-migration`→`isaac_jk`)의 결과를 리뷰한 것일 뿐 — 실제 Isaac Lab 코드는 `isaac_jk`에만 있음. (`Isaac-JS`의 `README.md`에도 이미 이 사실이 07-07에 명문화되어 있었음.)
- **`Isaac-JS`의 고유 연구 내용을 `isaac_jk`로 포팅 후 브랜치 삭제.** `notes/daily/daily_2026-07-05_gazebo_v15_regression.md`(파일명 변경 — `isaac_jk`가 같은 날짜에 별도 Isaac Lab 노트를 이미 갖고 있어 충돌 회피) + `daily_2026-07-07.md` 신규 추가, `exp_011` Postmortem 2 섹션 append, `rl_rules.md`에 Rule 25·26 신규(원래 `Isaac-JS`에서 Rule 16·20이었으나 `isaac_jk`가 이미 그 번호를 다른 내용으로 쓰고 있어 재번호). `00_index.md`/`RL_Project_Log.md` 허브 갱신. SAC pkl/mp4/zip 등 실수로 딸려온 대용량 파일은 의도적으로 미포함. → `isaac_jk` 커밋 `7d608c7`.
- **`Isaac-JS` 브랜치 삭제** (로컬 + `origin`).
- **`main`을 `isaac_jk`로 승격.** 승격 전, 구 `main`(junsang의 `_junsang` 접미사 연구노트 20여 개 + `donghyeok` 스타일의 초기 `isaac_lab_tasks/` 스켈레톤 보유)을 태그 `archive/main-pre-isaac_jk-promotion`으로 보존 후 `origin`에 push, 이후 로컬 `main`을 `isaac_jk`로 `reset --hard` → `--force-with-lease`로 `origin/main` 갱신.
- **`Issac_JS`(junsang, 오타 아님)는 사용자 지시로 미삭제·미변경.** 작업 도중 junsang이 `Issac_JS`에 새 커밋(`2d08794`, "v20 추가: v19_precise 사양 그대로 fresh 재학습용 task/flag 등록")을 푸시한 것을 확인 — 아직 `main`/`isaac_jk`에 미반영.

---

## 주요 결정 & 발견

- **`main`의 신규 기준 = `isaac_jk`.** 근거: 팀 자체 문서(`daily_2026-07-28_team_briefing.md`)가 이미 `isaac_jk`를 공식 upstream으로 못박아 둠 (`Issac_JS`→`isaac_jk` 병합 후 "Issac_JS로 직접 push 금지" 명시). `isaac_jk`가 이동 타겟/KF·exp_020/021 등 실제 진행 중인 작업의 유일한 소재지.
- **브랜치 이름 3종(`Isaac-JS`/`isaac_jk`/`Issac_JS`)의 유사성이 이번 혼선의 근본 원인.** 하이픈/언더스코어/오타 한 글자 차이로 서로 다른 브랜치를 가리켜 대화 중 여러 번 재확인이 필요했음.
- **force-push 전 태그 백업이 저비용 안전장치임을 재확인.** 20여 개 노트 파일을 일일이 재검토·포팅하는 대신, 태그 하나로 구 `main`을 통째 보존 — 필요해지면 그때 개별 파일을 꺼내 쓰면 됨.
- **git 저장소 용량 문제는 branch 정리로 해결되지 않음.** SAC replay buffer·YOLO 체크포인트·mp4가 여러 브랜치 히스토리에 실제로 박혀 있어(pack 320MB), 근본 해결은 `git filter-repo` 등 히스토리 재작성이 필요 — 오늘은 미실행(파괴적 작업, 별도 확인 필요).

---

## 코드 변경 사항

| 파일 | 변경 내용 |
|------|----------|
| `.graphifyignore` (신규) | graphify 지식그래프 제외 경로 — YOLO 데이터셋/영상/Obsidian 플러그인 |
| `CLAUDE.md` | graphify 섹션 추가 (조회 명령 표, 그래프 갱신 절차) |
| `.gitignore` | `graphify-out/` 추가 |
| `notes/daily/daily_2026-07-05_gazebo_v15_regression.md` (신규) | `Isaac-JS`에서 포팅 — v15 310K 회귀 진단 + GPU 드라이버 인시던트 |
| `notes/daily/daily_2026-07-07.md` (신규) | `Isaac-JS`에서 포팅 — 보상함수 10단계 변천사 + v16/Stage C 제안 |
| `notes/experiments/exp_011_wobble_lpf_reward_damping.md` | Postmortem 2 섹션 추가 |
| `notes/research/rl_rules.md` | Rule 25(댐핑 반경 vs 종단 반경)·Rule 26(GPU 드라이버 공유 자원) 신규 |
| `notes/00_index.md`, `RL_Project_Log.md` | 위 신규 노트·규칙에 대한 허브 포인터 갱신 |

커밋: `isaac_jk`/`main` 공통 `7d608c7` ("merge(Isaac-JS): port graphify setup + Gazebo/SAC track research (v15 regression, Rule 25/26)")

---

## 문제 & 해결

| 문제 | 해결 여부 | 메모 |
|------|----------|------|
| `Isaac-JS` push 시 "용량 초과" | ✅ | 원인=마지막 커밋의 `git add .`가 SAC pkl/mp4/zip ~166MB를 실수로 포함. 브랜치 자체를 삭제해 해소, 근본 히스토리 bloat는 별도 과제로 남김 |
| 이 저장소 worktree 파일 소유권(`gndbzl10`)으로 인한 쓰기 실패 | ✅ | `sudo chown` 임시 변경 → 편집·커밋·push → 원 소유권 복원 |
| `git reset --hard`/`checkout` 시 root 소유 산출물 파일 unlink 실패 | ✅ | 해당 디렉토리만 `sudo chown`으로 국소 처리 |
| junsang이 세션 도중 `Issac_JS`에 새 커밋 push (v20 등록) | ❌ open | `main`/`isaac_jk`에 아직 미반영 — 다음에 merge 필요 |
| git 저장소 히스토리 bloat(pack 320MB, SAC/YOLO/영상) | ❌ open | `git filter-repo` 등 히스토리 재작성 필요, 파괴적 작업이라 미실행 |

---

## 내일 할 일

- [ ] `Issac_JS`의 신규 커밋(`2d08794`, v20 task/flag 등록)을 `isaac_jk`/`main`에 merge
- [ ] (선택) git 히스토리 다이어트 — SAC replay buffer·YOLO epoch 체크포인트·mp4를 `filter-repo`로 제거할지 결정
- [ ] (선택) `donghyeok`/`junsang` 브랜치 처리 방침을 팀에 확인 (둘 다 아무 브랜치에도 머지되지 않은 고아 브랜치)
- [ ] `main` 기준으로 이후 작업 계속 진행

---

## 관련 노트

- [[00_index]]
- [[daily/daily_2026-07-05_gazebo_v15_regression]]
- [[daily/daily_2026-07-07]]
- [[research/rl_rules]] Rule 25 / Rule 26
- [[daily/daily_2026-07-28_team_briefing]]
