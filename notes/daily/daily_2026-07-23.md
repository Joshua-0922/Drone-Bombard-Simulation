---
date: 2026-07-23
tags: [daily, log]
type: daily
status: complete
---

# 연구 일지 — 2026-07-23

---

## 오늘 한 일

- **exp_020 — 물리 페이로드 부착 첫 학습 완주** (`isaac-verify`/L4, wandb `o5jn9xzk`):
  Phase 1 `--release_terminal`, 2048 envs × 400 iters, warm-start `exp018_B0_final.pt`,
  `--w_aim 1.0 --aim_reward_scale 0.5`(B0 보상 bit-match) → 페이로드가 유일한 델타.
  사전 스모크(16 envs × 2 iters) PASS 후 본 학습 ~21분 완주.
- **deterministic 200-ep eval**: success/release **100.00% (200/200)**, drop err mean
  0.169 m(max 0.200), 호버-드롭 유지(final speed med 0.068 m/s).
- **wandb 평가 figure 파이프라인 신설**: `play.py --wandb` — eval 요약 스칼라 + 분포
  히스토그램 5종 + 종단 원인 bar chart를 `job_type="eval"` run(`vryuc6mu`)으로 기록.
- 체크포인트 백업: `/opt/drone-bombard/checkpoints/exp020/exp020_payload_final.pt`
  (컨테이너 `/workspace/logs/isaac_lab/drone_bombard/exp020_payload_final.pt`).

## 주요 결정 & 발견

- **물리 페이로드(kinematic weld)의 학습 비용 = 0** — release_rate 첫 롤아웃부터 100%
  고정(B0의 23→99.6% 상승 커브와 대조), 재학습 과도기 없음. exp_019 설계 예측의
  학습-스케일 실증. Stage C는 페이로드 포함 상태로 진행 가능.
- **σ 드리프트 관찰**: noise_std 1.41→1.71(B0 ~1.35 안정) — release 100% 포화로 sharpen
  압력 부재. 현 규모 무해, 장기 fine-tune 회귀 시 1순위 확인 지표.
- drop err 분포가 tolerance 경계 쪽 이동(mean 0.125→0.169, med 0.178) — referee 정의상
  정상이나 σ 드리프트와 같은 방향성.
- **컨테이너 baked-in `WANDB_API_KEY`가 빈 값** → 1차 기동 silent 실패(isaaclab.sh exit 0
  삼킴). `--env-file /opt/drone-bombard/.wandb.env` 필수. → [[errors/err_20260723_wandb_key_empty]]

## 코드 변경 사항

- `isaac_lab/play.py`: `--wandb/--wandb_project/--wandb_run_name` 플래그 +
  `_log_eval_to_wandb()`(summary 스칼라·히스토그램·종단 bar chart). 기존 출력 경로 불변.

## 문제 & 해결

- wandb 키 공백 silent 실패 → 로그 grep으로 검출, `--env-file` 주입으로 해결
  ([[errors/err_20260723_wandb_key_empty]]).
- 워크트리 파일 gndbzl10 소유 → sudo chown 편집 후 1006:1007 복원 절차 준수.

## 내일 할 일

- Stage C(DR+residual, **별도 지시 대기**) — warm-start `exp020_payload_final.pt` 또는
  `exp018_B0_final.pt`, Phase-2 DR 힘 정합(물리 페이로드 낙하에 drag/wind 미적용) 선행 필요.
- σ 드리프트 모니터링(장기 fine-tune 시).

## 관련 노트

- [[experiments/exp_020_o5jn9xzk_payload_training]] / [[experiments/exp_019_physical_payload]]
- [[research/physical_payload_attach]] / [[errors/err_20260723_wandb_key_empty]]
- [[experiments/training_history]] / [[00_index]]
