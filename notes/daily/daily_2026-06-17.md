---
date: 2026-06-17
tags: [daily, throughput, arming, EKF, arm_bail_timeout]
status: done
type: daily
---

# Daily 2026-06-17 — v13 처리량 병목 진단 & arm_bail 수정

## 오늘 한 일

- v13(`rl_yolo_v13_terminal_reward`, 46y4xtiw) 학습 로그 점검 → 처리량 진단.
  - ~10h에 29.9K(6%)뿐, fps≈0.83, ETA ~6.5일. ep_rew≈95로 plateau.
  - 지배적 싱크: `PX4 not armed after 10s` early-bail (210회 / 401 infra restart).
- 근본 성격 규명: `ARM REJECTED` **0회** → v12 게이팅이 실제 거부는 제거. 실제는 EKF가 10s 내 재수렴 실패.
- 컨트롤러 계측 추가(`PREFLIGHT-PASS` dt 로깅) + 격리 dry-run(`arm_bail_timeout=25s`, offline) 실행.
- 결과로 `hyperparams_v13.yaml` `arm_bail_timeout` 10→20 수정. 노트/허브/로그/메모리 갱신.

## 주요 결정 & 발견

- **EKF 재수렴은 bimodal:** teleport 후 `pre_flight_checks_pass` 첫 True까지 **0.0s(7/12, warm) 또는 13–16s(5/12 ≈ 42%, stuck-EKF)**. 25s 창에서 **bail 0 / SUCCESS 4**.
- **v12의 10s 컷이 진짜 병목:** recoverable-with-time(13–16s)을 복구 직전 3–6s에 단두대질 → 멀쩡한 PX4 버리고 ~25–30s full restart 강제.
- **Fix:** `arm_bail_timeout` 10→20 (max 관측 15.7s + 마진). 진짜 죽은 인프라는 여전히 20s fast-fail.
- **결정:** v13는 SIGTERM emergency save(30K + 리플레이) 보존 후 중단. arm_bail은 보상식 아니므로 `--resume`으로 30K 재개 가능 (Fresh Start 불필요).

## 코드 변경 사항

- `drone_controller/drone_controller/drone_controller_node.py`: `_connect_time` 추적 + `pre_flight_checks_pass` 첫 True 시 `PREFLIGHT-PASS: ...{dt}s after PX4-connect` 1회 로깅 (계측). colcon build 완료.
- `rl_navigation/config/hyperparams_v13.yaml`: `arm_bail_timeout: 10.0 → 20.0`.
- `rl_navigation/config/hyperparams_v13_armdiag.yaml`: 신규 (진단용 격리 config — 25s, 별도 checkpoint_dir, offline).

## 문제 & 해결

- **문제:** `/tmp/ctrl_0.log`가 에피소드마다 `'w'` truncate → PREFLIGHT-PASS 타이밍 소실.
  - **해결:** `tail -F`로 append-only `preflight_timings.log` 수집기 구성.
- **문제:** dry-run이 v13와 같은 instance 0/port 8888 + 전역 `pkill drone_controller` → 동시 실행 시 v13 학습 노드 사망 + EKF 타이밍 측정 오염.
  - **해결:** v13를 먼저 graceful stop(SIGTERM, emergency save 확인) 후 단독 dry-run.
- **문제:** 격리 안 하면 fresh start가 v13 체크포인트(`sac_drop_*_steps.zip`) 삭제(train_sac L379-382).
  - **해결:** armdiag config에 별도 `checkpoint_dir` 지정.

## ⚠️ 인시던트 — armdiag dry-run이 v13 30K 체크포인트 파괴

30K 재개를 시도하다 발견: armdiag dry-run이 격리 실패로 **메인 체크포인트 dir에서 실행**되어 v13 30K(+주기 체크포인트 5개) 삭제 + preempt를 599-step으로 덮음. **30K 디스크 복구 불가.**
- **근본 원인:** `hyperparams_v13_armdiag.yaml`의 `training:`에 `checkpoint_dir`가 **중복**(내가 기존 키 위에 추가) → YAML last-wins로 메인 경로가 이김. + train_sac fresh-start의 무조건 `*_steps.zip` 삭제(L379).
- **재발방지:** ① 기존 키는 *편집*(추가 전 `grep`), ② 파괴적 fresh-start 전 startup `Checkpoints : <dir>` 로그로 격리 검증, ③ 진단 dry-run은 별도 부모 dir.
- 상세: [[errors/err_20260617_dryrun_clobbered_v13_checkpoints]]

## v13 fresh 재시작 + 프로덕션 검증

- 사용자 결정으로 **fresh 재시작**(iyhfy5ps, arm_bail=20, 0→500K). startup 정상(no Resuming, PX4 ready, 에러 0). 새 wandb run(46y4xtiw 미오염).
- **검증:** 초기 윈도우(7 infra starts) **bail 0 / SUCCESS 3 / TRACKING t/o 0**. PREFLIGHT-PASS: 0.0s×7, **14.1/14.8/15.5s ×각1 전부 회복** — 구 10s면 30% bail. fix 프로덕션 작동 확인.

## 내일 할 일

- [ ] v13(iyhfy5ps) 추세 점검: success_rate 발생, ep_len/env/ep_reward, 전체 bail율(구 ~21/h 대비).
- [ ] (장기) teleport reset이 EKF를 13–16s 흔드는 원인 — 명시적 EKF reset 등으로 재수렴 단축 검토.
- [ ] OPEN: YOLO target_lost ~29%.

## 관련 노트

- [[experiments/exp_006_xgzum51v_armdiag_dryrun]]
- [[research/cruise_timeout_arming]] (06-17 정정 섹션)
- [[research/rl_rules]] — Rule 11
- [[experiments/training_history]] / [[00_index]]
- [[daily/daily_2026-06-16]]
