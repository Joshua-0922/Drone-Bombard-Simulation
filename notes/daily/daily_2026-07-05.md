---
date: 2026-07-05
tags: [daily, isaac-lab, physics-fix, ablation]
status: done
type: daily
---

# Daily 2026-07-05

## 오늘 한 일

- 확정 버그 2건 기계적 수정 + 커밋: 속도킥(스폰타임 `UsdPhysics.MassAPI` authoring,
  `cd0c617`), 로터 스핀 리셋 재주입(`init_state` 오버라이드, 동일 커밋),
  `action_sat_frac` 메트릭(`7657df4`)
- `--zero-actions` 하드 게이트 **PASS** (11.9 m FAIL → 0.18-0.22 m)
- Probe A/B/C 실행·교차검증 → **A1 생략, A2 직행** 판정
- **inertia 대반전 계측** (`_diag_inertia.py`): set_inertias는 전파됨 → exp_013은
  rate loop ~1300× 저토크 plant. x500 inertia authoring으로 일관 plant (`7d0e9b6`)
- `yolo_eval.py --calibrate` 하네스 전면 수리(`5343e38`) — 실행은 컨테이너
  annotator 버그로 차단(stock 예제 동일 크래시), 분석 커브로 진행
- 비전 slant-range 감쇠 구현·적용 (`range_falloff_enabled`)
- **exp_014 학습 2회**: A2(감쇠 ON, `v3qk07pg`) + A0′(감쇠 OFF 대조, `azoc1xp0`),
  각 400 iters + A2 deterministic 200-ep eval

## 주요 결정 & 발견

- **A2: R_alt(300-400)=0.0000, eval 100.0% (202/202)** — exp_013 36% 대비.
  climb 창발(iter 150-199, 27.8%) 후 50 iter 내 완전 기각 시그니처.
- **A0′: R_alt 0.0365, success 96.5%** — exp_013 규모 고착은 재현 안 됨.
  귀속: **지배 요인 = plant 일관성**(빠른 완주가 farming 압도), 감쇠 = 잔여
  꼬리 제거 + YOLO parity(유지 결정).
- noise_std 두 arm 모두 0.76-0.80 안정 — exp_013의 σ 폭주도 plant 아티팩트
  (Rule 18b 재해석).
- 07-04 포렌식의 "set_inertias 전파 안 됨" **정정**; 구 정책은 plant-overfit
  (재평가 bad_att 68%, 프롭스핀 A/B로 로터 무관 실증) → Rule 19 신설.
- reward_success·entropy_coef는 사용자 지시로 불변 유지.

## 코드 변경 사항

- `drone_bombard_env.py`: `_author_body_mass_props`(스폰 authoring), init_state
  joint_vel 0, 런타임 override 제거+가드, `action_sat_frac`, range falloff
- `yolo_eval.py`: X-마커 쿼드, `_setup_scene` 카메라 패턴, PinholeCameraCfg,
  스윕 하드닝, `--range-max`
- 신규: `_diag_inertia.py`, `_probe_traj.py`, `_probe_analyze.py`,
  `_fit_falloff.py`, `_extract_traincurve.py`
- 커밋: `cd0c617`, `7657df4`, `7d0e9b6`, `5343e38` + docs

## 문제 & 해결

- TiledCamera annotator 크래시(이미지 레벨) → stock 예제로 격리, 차단 기록
- wandb 키 부재 → 호스트 `/opt/drone-bombard/.wandb.env`를 `--env-file`로 주입
- `simulation_app.close()` 행 프로세스 3개가 CPU 잠식 → kill 절차 메모리에 기록

## 내일 할 일

- (사용자 결정) 다음 페이즈: reward_success 300 / entropy_coef — 단 noise_std가
  일관 plant에서 안정이므로 entropy 근거 재평가 필요
- success_radius 0.8→0.5 커리큘럼 후보 (현 100%는 0.8 기준)
- 이미지 수정 후 `yolo_eval.py --calibrate` 실측 커브 교체
- 임무 지표(drop_impact_error 4.59 m) 개선 트랙 검토

## 관련 노트

- [[experiments/exp_014_A2_visionrange]] / [[research/isaac_inertia_ctrl_mismatch]]
- [[research/rl_rules]] Rule 19 / [[research/exp014_ablation_protocol]]
- [[sessions/session_2026-07-05]] / [[00_index]]
