---
date: 2026-07-28
tags: [git, merge, junsang]
status: done
type: daily
---

# Daily 2026-07-28

## 오늘 한 일
- `origin/Issac_JS`(준상 브랜치) 최신 6커밋을 `isaac_jk`에 머지 (머지 커밋 `a099de3`)
  - v19 collapse fix (A+B) + best-checkpoint retention + viz (`2f2bf9b`)
  - v19 precision push: continuous landing reward toggle (`09a53a2`)
  - v19 warm-start 체크포인트 공유: `checkpoints/v19/{precise,abd}/model_{best,final}.pt` + `WARMSTART.md` (`cc522e3`)
  - `isaac_lab/select_best_checkpoint.py` 신규, `REVIEW_GUI.md` 신규, README 갱신

## 주요 결정 & 발견
- 로컬 미커밋 변경(v-track 삭제 + 07-24 렌더 작업)은 **바이트 동일하게 보존** — stash `pre-merge-IssacJS backup 2026-07-28` + 패치 백업
- `drone_bombard_env.py` / `play.py` / `v11_env.py`(삭제) 3개 파일은 워킹트리가 준상 버전을 **가리고 있음** — 준상 버전 필요 시 `git checkout -- <file>`
- push 안 함: `isaac_jk`의 upstream이 `origin/Issac_JS`라 `git push`가 준상 브랜치로 나감 — 주의

## 코드 변경 사항
- 커밋: 머지 커밋 `a099de3`만 (워킹트리 변경은 미커밋 유지, 사용자 재가 대기)

## 문제 & 해결
- 워크트리 파일 gndbzl10 소유 → sudo git으로 머지 후 소유권 원복 (worktree→gndbzl10, .git→nachoigpt)

## 내일 할 일
- v-track 삭제 커밋 여부 결정 (사용자)
- 가려진 3개 파일에서 준상 v19 수정 채택 여부 결정

## 관련 노트
- [[daily/daily_2026-07-24]]
- [[00_index]]

## 추가 (같은 날, 후속 지시)
- 셰도잉 3파일(`drone_bombard_env.py`, `play.py`, `v11_env.py`) → `git checkout --`으로 **준상 머지 버전 채택** (구 로컬 버전은 stash@{0}에 보존)
- `git push -u origin isaac_jk:isaac_jk` 완료 (8d75aa7..a099de3) — **upstream이 origin/isaac_jk로 변경**되어 이후 `git push` 안전
- 남은 미커밋: `__init__.py`/`train.py`/`math_utils.py`/`test_math.py`(v-track 제거 stale) + `record_episode.py`/`00_index.md`(07-24 렌더 작업) — 처리 방침 미정

## 추가 2 (이동 타겟 + KF 트래커 구현)
- base env에 X마커 모션 모델 3종 추가: `--target_motion cv/ca/ct` (+기존 `gm` 기본값 유지), `--moving_target`(phase 무관 강제 ON), 속도/가속/선회율 파라미터
- Singer(Gauss-Markov 가속) Kalman 트래커 `--target_kf`: YOLO 노이즈 픽셀 → 지면 역투영 측정 → obs 14→21 (KF 상대위치/속도/가속/validity)
- 단위테스트 20종 신규(총 57 PASS, isaaclab.sh -p pytest), isaac-verify 스모크 4종 PASS (kf_pos_err 0.09–0.12 m)
- 상세: [[research/moving_target_models]] — **--target_kf는 기존 ckpt warm-start 불가(Fresh Start 필수)**

## 관련 노트 (추가)
- [[daily/daily_2026-07-28_team_briefing]] — 팀 공유용 브리핑 (현황 + 각자 할 일)
