---
date: 2026-07-03
tags: [daily, log, isaac-lab, migration, ppo]
type: daily
status: complete
---

# 연구 일지 — 2026-07-03

---

## 오늘 한 일

- **Isaac Lab migration Phase 2 — env + PPO 코드 이식 완료.** Gazebo+PX4+ROS2 SAC 태스크를
  Isaac Lab DirectRLEnv + rsl_rl PPO로 포팅.
- 별도 워크트리 생성: `git worktree add /opt/drone-bombard/isaac-worktree feat/isaac-env-migration`
  + `git merge jekyun`(merge `940c88b`) — jekyun 체크아웃의 라이브 v15 SAC 학습(tmux `rl_train`)
  방해 없이 작업.
- 병합 충돌 2건 해결: `notes/00_index.md`(현재 상태 — 병행 트랙으로 병합), `notes/daily/daily_2026-05-30.md`
  (add/add — 별개 세션 노트를 [[daily/daily_2026-05-30_vision-refactor]]로 분리 보존).
- `isaac_lab/` 신설:
  - `drone_bombard/math_utils.py` — 순수 torch action rate-limit/LPF, pinhole vision 투영,
    hold-buffer, ballistic/CCIP, 3-layer reward, overshoot/stagnation guard. isaaclab 무의존
    (import 체인 분리 — 로컬 유닛테스트를 위한 의도적 구조).
  - `drone_bombard_env.py` — DirectRLEnv, 위 math_utils를 isaaclab lifecycle(scene/actuation/
    obs/dones/rewards/reset)에 연결. 캐스케이드 속도→자세→토크 컨트롤러(PX4 모방, 단일
    rigid-body wrench).
  - `mdp/domain_rand.py`, `agents/rsl_rl_ppo_cfg.py`, `train.py`, `play.py`, `yolo_eval.py`,
    `tests/test_math.py`, `README.md`.
- `infra/startup.sh`에 repo/logs 마운트 + `WANDB_API_KEY` 전달 + Cartpole 스모크 게이트 후
  drone_bombard PPO 학습 자동 기동 추가. `requirements.txt`에 `ultralytics` 추가(yolo_eval용).
- 유닛 테스트 실행: `pytest isaac_lab/tests/test_math.py` — **29/29 통과**(dev VM에 isaaclab
  없어 `drone-bombard-harmonic` 컨테이너의 torch 2.4.1로 실행, 파일 경로 직접 로드로
  `drone_bombard/__init__.py`의 isaaclab import 체인 우회). 초기 3건 실패는 전부 내 테스트
  손계산 상수 오차 — 수정 후 전부 통과.
- 문서: `notes/experiments/exp_012_isaac_migration_phase2.md`(parity 표 포함),
  `notes/research/isaac_velocity_controller.md`, `notes/research/rl_rules.md` Rule 16 신규,
  `notes/experiments/training_history.md` + `notes/00_index.md` + `RL_Project_Log.md` 갱신.

---

## 주요 결정 & 발견

- **Vision: 학습=analytic pinhole(YOLO 캘리브레이션 노이즈), 평가=실제 YOLOv8 이원화.** obs
  인터페이스(u_norm, v_norm, conf)는 두 경로가 동일 — real-camera 렌더링을 학습 루프에 넣으면
  Isaac Lab의 병렬화 이점(수천 env)이 죽으므로.
- **Drop = 액션 아님, 스크립트 CCIP 메트릭.** v15와 태스크 스코프 동일 유지(비교 가능성).
  Phase 2 훅(learned residual)만 비활성 스텁으로 wiring.
- **PPO 라이브러리 = rsl_rl** (Isaac Lab 기본, Docker 이미지 기설치, GPU-vectorized).
- **`limit_tilt: 0.26`(Gazebo yaml)은 죽은 설정** — 코드가 어디서도 참조 안 함. 실제 전도
  판정은 `limit_inverted_tilt=1.047`(yaml 미설정, 코드 기본값). Isaac은 1.047만 이식(정확).
- **`_filter_velocity`의 tau 관례 재확인**: 코드 주석은 `τ≈dt·(1/α-1)≈75ms`(20Hz,α=0.4) —
  이전 세션의 "97.9ms 보정"은 다른(틀리지 않았지만 코드베이스 관례와 다른) 연속시간 근사였음.
  이산 재귀식 자체는 어느 쪽이든 동일 — 라벨링만 코드 관례로 통일.
- **Overshoot guard가 success_radius=0.8에서 도달 불가능한 것은 버그 아님.** Gazebo 소스
  (`drone_drop_env.py:775-835`) 재확인 결과 원본도 동일하게 dormant — Rule 10의 의도된 설계
  (0.5 curriculum 전환 시 활성화). 이식 시 "고쳐서" arm_radius를 낮췄다면 Rule 10이 막았던
  정상-접근 오탐을 재도입했을 것 — **Rule 16으로 일반화**(시뮬레이터 이식 시 dormancy는
  버그 추정 전에 원본 소스로 먼저 검증).
- **추력 계산 라벨 오류 발견·정정**: T/W=2.0은 무탑재(2.07kg) airframe 고정 스펙인데,
  탑재질량(2.17kg) 기준으로 잘못 라벨링해 42.6N이 나왔었음 → 40.6N으로 정정. 실측 데이터
  아님(SITL 추정), 컨트롤러 accel clamp가 먼저 발동해 실제 상한 미도달 확인.

---

## 코드 변경 사항

| 파일 | 변경 내용 |
|------|----------|
| `isaac_lab/drone_bombard/math_utils.py` | 신규 — 순수 torch action/vision/ballistic/reward/guard 함수 |
| `isaac_lab/drone_bombard/drone_bombard_env.py` | 신규 — DirectRLEnv, cfg 그룹, 캐스케이드 속도 컨트롤러 |
| `isaac_lab/drone_bombard/mdp/domain_rand.py` | 신규 — Phase-2 DR 스텁(항등) |
| `isaac_lab/drone_bombard/agents/rsl_rl_ppo_cfg.py` | 신규 — PPO 하이퍼파라미터 |
| `isaac_lab/train.py` / `play.py` / `yolo_eval.py` | 신규 — 학습/sanity-check/YOLO-eval 엔트리포인트 |
| `isaac_lab/tests/test_math.py` | 신규 — 29개 순수 torch 유닛 테스트 |
| `isaac_lab/README.md` | 신규 — 실행 절차, parity 요약, 테스트 안내 |
| `infra/startup.sh` | repo/logs 마운트, WANDB_API_KEY 전달, Cartpole→drone_bombard 2단계 게이트 |
| `drone_drop_system/docker/requirements.txt` | `ultralytics` 추가 |
| `notes/00_index.md`, `notes/experiments/training_history.md`, `notes/research/rl_rules.md`, `RL_Project_Log.md` | Isaac migration Phase 2 문서화 반영 |

---

## 문제 & 해결

| 문제 | 해결 여부 | 메모 |
|------|----------|------|
| `drone_bombard_env.py`에 순수 함수를 두면 `tests/test_math.py`가 isaaclab 없이 import 불가 (모듈 최상단에서 isaaclab import) | ✅ | `math_utils.py`로 순수 함수 전부 분리; 테스트는 파일 경로 직접 로드로 패키지 `__init__.py` 우회 |
| `project_target_pinhole`이 함수 내부에서 `isaaclab.utils.math.quat_apply_inverse` import — 테스트 불가 | ✅ | 순수 torch 쿼터니언 회전(`quat_apply_inverse_pure`) 자체 구현 |
| 초기 유닛테스트 3건 실패 | ✅ | fx 상수를 손으로 근사한 값과 실제 tan(0.5235) 결과가 달랐음(60°가 아닌 1.047rad) — `_fx()` 헬퍼로 유도값 사용하도록 수정. LPF per-env 격리 테스트의 잘못된 비교 로직도 수정 |
| worktree 병합 충돌 2건 (00_index.md, daily_2026-05-30.md) | ✅ | 위 "오늘 한 일" 참조 |
| 이 dev VM에 isaaclab 설치 불가(GPU driver 535<580) → env 코드 실행 검증 불가 | ❌ 미해결 (설계상 불가피) | 순수 math는 유닛테스트로 검증; env wiring(scene/actuation/API 이름)은 L4 VM 첫 스모크에서 검증 필요 — README에 알려진 리스크로 명시 |

---

## 내일 할 일

- [ ] L4 Spot VM 기동 (`infra/deploy.sh` 빌드+push) → Cartpole 스모크 → `Isaac-DroneBombard-Direct-v0` 2-iter 스모크
- [ ] `play.py --zero-actions`/`--scripted` 물리 검증 (hover drift < 1m, d_xy 단조 감소)
- [ ] PX4 속도 스텝응답 Gazebo 캡처 세션(`vel_logger_v2.py` 신규, 7-포인트) — v15 학습 일시정지 중 또는 완료 후
- [ ] `yolo_eval.py --calibrate` 첫 실행 → vision 캘리브레이션 v1
- [ ] `feat/isaac-env-migration` 커밋 + push

---

## 관련 노트

- [[experiments/exp_012_isaac_migration_phase2]]
- [[research/isaac_velocity_controller]]
- [[research/rl_rules]] Rule 16
- [[00_index]]
