---
date: 2026-09-13
tags: [daily, log, residual, l1, rl, preflight]
type: daily
status: complete
---

# 연구 일지 — 2026-09-13

> **한 줄.** L1-RL(잔차 강화학습)을 L1-SL과 비교하기 전에 코드를 감사했다.
> **수정 없이 돌리면 비교가 무효가 되는 결함 4건 + 새로 찾은 1건.** 코드는 건드리지 않았다.

---

## 오늘 한 일

- L1-RL 사전점검 → [[research/l1_rl_preflight]]
  - `task_env.py` 잔차 경로(`_ccip` · `_pre_physics_step` · `_get_rewards` · `_get_dones`)와
    `train.py::_prepare_residual_head`, `play.py::_SLResidual`/`_load_policy`, rsl_rl 3.1.2 내부
    (`OnPolicyRunner.load` · PPO 옵티마이저 생성 · `ActorCritic.std`)를 읽었다.
- 컨테이너 복구: `isaac-verify`가 6일 전 Exited(137) → `docker start`. `/tmp/rebuild`는
  repo와 **전 파일 md5 일치**. GPU 비어 있음. L0 seed 3은 model_50까지만 있는 미완 run.

## 주요 결정 & 발견

1. **비교의 전제 = 학습 신호만 다르고 나머지는 전부 같다.** 동결 L0 seed 1 · 잔차 입력 38채널 ·
   같은 망 구조 · `scale` 2.0 · EMA 0.3 · 같은 평가 시드. 현재 코드는 이 중 **아무것도** 만족하지 않는다.
2. **정보 불공정이 가장 큰 함정이다.** RL 잔차 입력은 obs 26채널뿐인데, 같은 정보량의 SL 팔은
   이미 L0에게 진다(CEP50 +7.8%). 이대로 돌리면 "RL이 안 된다"가 아니라 "정보를 덜 줬다"를 잰다.
3. **동결 팔은 별도 잔차망이어야 한다.** 출력층 행만 학습시키는 현행 `--freeze_nominal`은
   새 입력 채널을 볼 수 없다(첫 층이 얼어 있다). Johannink식 합성 액터 + 옵티마이저 재생성.
   부산물로 `res_gi.pt` 가중치 초기화가 가능해져 "종단 보상이 예측 위에 더하는가"를 직접 잰다.
4. ⚠️ **새 발견:** `runner.load()`가 Adam 모멘텀을 복원하므로 그래디언트를 0으로 마스킹해도
   동결 행이 미세하게 움직인다(`p.grad is None`일 때만 건너뛴다). `test_residual_head`는 빈 Adam으로
   검사해서 못 잡는다. `load_optimizer=False`로 해결.
5. 08-30 조준 보상 결함은 여전히 미수정. `gate_reward`·`drop_signal_reward`는 farming 불가임을 확인 —
   **조준 항 하나만 고치면 된다.**

## 코드 변경 사항

| 파일 | 변경 내용 |
|------|----------|
| (없음) | 감사만 수행. 수정 목록은 [[research/l1_rl_preflight]] §1 |
| `notes/research/l1_rl_preflight.md` (신설) | 필수 수정 6건 · 파라미터 결정 · 인프라 점검 · 실행 순서 |

## 문제 & 해결

| 문제 | 해결 여부 | 메모 |
|------|----------|------|
| `isaac-verify` 정지 상태 | ✅ | `docker start`. 산출물 전부 보존 확인 |
| `train.py` 주석의 rsl_rl "v2.3.2" | 메모 | 실제 3.1.2. `runner.alg.policy` 등 가정은 유효 |
| 동결 행 Adam 모멘텀 누출 | ⚠️ 미수정 | 위 발견 4 |

## 내일 할 일

- [ ] [[research/l1_rl_preflight]] §1 순서대로 수정 (1.1 → 1.3 → 1.2 → 1.4/1.5/1.6), `docker cp`, 단위테스트
- [ ] 누적 채널 등가 검사 1회 + dry-run 3 iter
- [ ] 파일럿 2팔 × 500 iter (0 초기화 / `res_gi.pt` 초기화), seed 1
- [ ] `_gi_headline.sh` 조건으로 평가 → `_agg_table1.py` 한 표

## 관련 노트

- [[research/l1_rl_preflight]] · [[research/residual_ceiling]] · [[research/release_gate_jitter]]
- [[errors/err_20260830_aim_reward_residual_inclusive]] · [[research/rl_rules]] · [[00_index]]
