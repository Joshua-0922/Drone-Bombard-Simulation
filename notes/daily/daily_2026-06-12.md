---
date: 2026-06-12
tags: [daily, log, EKF, YOLO, proximity, debug]
type: daily
status: complete
---

# 연구 일지 — 2026-06-12

> ⚠️ **2026-06-14 정정:** 이 날의 핵심 진단인 **"EKF East 축 반전"은 오진**이었음.
> 실제 PX4 East = +Gazebo_East (반전 없음). 여기서 적용한 `_target_ned=[10,-11]`,
> `TARGET_ENU_X=-11`은 드론을 마커의 **거울상 위치(-11,10)**로 보냈고, d_xy 로그가
> 거울상에서 자기일치하여 "수정됨"으로 오인됨 (당시 GUI 미사용). YOLO 미탐지도 이 때문.
> 올바른 분석·수정은 [[daily/daily_2026-06-14]] / [[coordinate-frames]] 참조.
> (아래 기록은 당시 작업의 역사적 기록으로 보존.)

---

## 오늘 한 일

- **YOLO conf 필터 버그 수정**: Boolean 텐서 인덱싱(`detections[bool_tensor]`) silent fail 발견 → `detections[:0]` 정수 슬라이스로 대체
- **두 install 경로 문제 발견 및 수정**: `ros2 run`이 실행하는 entry-point(`lib/vision_detection/xmarker_detector`)와 site-packages 모듈이 별개임을 확인. 이전 모든 수정이 site-packages에만 적용되어 실제로 동작하지 않았음. 두 경로에 모두 배포하는 절차 확립.
- **YOLO 이중 노드 문제 수정**: 이전 세션 수동 재기동 + env 자동 기동이 겹쳐 2개의 YOLO 노드가 `/target/pixel_coords`에 퍼블리시 중. PID 539114 (수동 기동) kill.
- **공간 필터(spatial filter) 구현**: 탐지 bbox 중심이 이미지 중심에서 200px 초과 시 거부. 첫 false positive (bbox_cx=25, 341px) 차단 확인.
- **stable_detect_ticks = 15로 올렸다가 다시 5로 복원**: 처음엔 FP 방지를 위해 올렸으나, 실제 마커 탐지도 1-2 tick에 불과해 TRACKING이 전혀 안 됨. 공간 필터가 FP를 막으므로 5로 복원.
- **치명적 버그 발견: EKF East 반전** — 8시간 학습 실패 원인 규명 및 수정
  - PX4 SITL Gazebo Harmonic: EKF East 축 = -Gazebo_East
  - `_target_ned = [10, 11]` → 실제로 Gazebo NW 22m 지점 (마커와 완전 다른 방향)
  - `_target_ned = [10, -11]`로 수정 → d_xy=3.9m PROXIMITY TRIGGER 즉시 확인

---

## 주요 결정 & 발견

### EKF East 반전 패턴 확인

PX4 SITL + Gazebo Harmonic 조합에서 EKF East = -Gazebo_East. `drone_controller_node.py`는 line 72에서 의도적으로 y를 반전하여 이를 보정:

```python
self.target_pos = [msg.x, -msg.y, -msg.z]
```

이 반전이 있기 때문에 NED(+1,+1) 순항 명령이 실제 Gazebo NE 방향 비행으로 이어짐. 그러나 proximity target 좌표는 이 보정을 반영하지 않아 8시간 동안 proximity가 한 번도 작동하지 않음.

**좌표 변환 공식:**
```
Gazebo ENU (East=X, North=Y) → EKF NED (North=Y, East=-X)
마커 Gazebo ENU (11, 10) → EKF NED (10, -11)
```

### YOLO 실제 마커 탐지 conf

드론이 마커 직상방 통과 시: conf **0.10–0.35** (낮고 단발적). False positive와 conf 범위 겹침 → conf 단독으로는 구분 불가. 공간 필터(pixel_dist ≤ 200px)가 핵심 차별자.

### RL env가 YOLO를 직접 관리함

`drone_drop_env.py`가 YOLO를 `_infra_procs`로 spawn·kill 관리. 수동으로 추가 기동 시 이중 노드 문제 발생 → `_infra_procs` 밖의 YOLO는 env가 kill하지 못해 영속적 중복 발생.

---

## 코드 변경 사항

| 파일 | 변경 내용 |
|------|----------|
| `mission_manager/mission_manager_node.py` | `_target_ned = [10, -11]` (EKF East 반전 수정), `stable_detect_ticks = 5`, 공간 필터 추가 |
| `vision_detection/monocamera_xmarker_detector.py` | conf 임계값 하드 게이트, `detections[:0]` 필터, 첫 탐지 프레임 저장, 두 install 경로에 배포 |

커밋: `a64ae89` (fix: correct proximity trigger NED coords for EKF East reversal)

---

## 문제 & 해결

| 문제 | 해결 여부 | 메모 |
|------|----------|------|
| YOLO 필터 silent fail (Boolean 인덱싱) | ✅ | `detections[:0]` 슬라이스로 대체 |
| entry-point vs site-packages 두 경로 | ✅ | 두 경로에 모두 cp하는 절차 확립 |
| 이중 YOLO 노드 (2×publisher) | ✅ | 수동 기동 노드 kill; 이후 env 단독 관리 |
| proximity trigger 8시간 미작동 | ✅ | `_target_ned = [10, -11]`로 수정; d_xy=3.9m 확인 |
| stable_detect_ticks=15로 YOLO 탐지 미발화 | ✅ | 5로 복원; proximity가 primary trigger |
| 128 에피소드 전체 stagnation (ep_rew=-174) | ✅ | proximity 수정 후 다음 에피소드부터 TRACKING 도달 |

---

## 세션 후반: rl_yolo fresh training 시작 (13:24)

이전 rl_yolo_debug 런 (WandB: `esmtny0a`, 128 에피소드, 33K steps) 폐기.
RL env에도 동일한 EKF East 반전 버그 발견:
- `drone_drop_env.py`: `TARGET_ENU_X = 11.0` → `-11.0`
- `hyperparams.yaml`: `target_enu_x: 11.0` → `-11.0`
- d_xy가 19–22m에서 3.2–3.7m으로 수정됨

**Fresh run** `rl_yolo` (WandB: `45l8vkw5`), 2M timesteps, nohup으로 실행 중 (터미널 종료해도 지속).

### 초기 8 에피소드 검증 결과

| 항목 | 결과 |
|------|------|
| TRACKING 진입 d_xy | 3.2–3.7m (9에피소드 중 7개) ✅ |
| EKF drift 에피소드 | 2/9 (d_xy=16.8m, 19.0m) ⚠️ |
| YOLO 마커 탐지 | step 186, conf=0.25, d_xy=2.1m ✅ |
| ep_rew_mean | -151 (4ep) → -181 (8ep) ⚠️ |
| fps | 2 (CRUISE timeout infra 재시작으로 느림) ⚠️ |
| nohup 실행 여부 | ✅ (PID 1370970, tty=?) |

EKF drift 에피소드는 기존 문제. 핵심 수정은 확인됨.

---

## 내일 할 일

- [ ] WandB `45l8vkw5` 런 ep_rew_mean 추세 확인 — 100+ 에피소드 후 개선 여부
- [ ] EKF drift 에피소드 비율 추적 (현재 ~22%)
- [ ] 50+ 에피소드 후에도 ep_rew_mean < -150이면 EKF drift 방어 로직 추가 검토
- [ ] fps=2 원인 분석 — CRUISE timeout 빈도 감소 방법

---

## 관련 노트

- [[research/ekf_east_reversal]] — EKF East 반전 패턴 상세 문서
- [[experiments/exp_004_rl_yolo_debug_vision]] — 이 세션의 실험 기록
- [[research/rl_rules]] — Known Failure Modes 갱신 (stagnation, YOLO 이중 노드)
